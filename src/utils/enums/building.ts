export enum BUILDING_STATUS {
    DEFAULT = 'default',
    DESTROYED = 'destroyed',
    ONGOING = 'ONGOING',
}

export enum BUILDING_SIZE {
    BIG = 4,
    SMALL = 1,
}

export enum BUILDING_TYPE {
    REFINERY = 'refinery',
    WALL = 'WALL',
}

export enum BUILDING_ACTION {
    REFINE = 'refine',
    PRODUCE = 'produce',
    NONE = 'none',
}